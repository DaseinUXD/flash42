package {
	
	import flash.display.MovieClip;
	import flash.display.Shape;
	import flash.events.MouseEvent;
	import flash.filters.DropShadowFilter;
		
	public class Domino extends MovieClip {

		

			
			private var isUp:Boolean;
			private var isDouble:Boolean;
			
			private var numDouble:uint;
			private var numHighSide:uint;
			private var numLowSide:uint;

			private var isCounter:Boolean;
			private var numCounter:uint;
	}
}
			

		/*/initiallizes... initiallization takes place within the class constructor
		public function Domino() {
			
			this.isUp = up;
			this.isDouble = isDbl;
			this.numDouble = dbl;
			this.numHighSide = hi;
			this.numLowSide = low;
			this.isCounter = isCount;
			this.numCounter = count;
			
		
		}
		
		var Domino:Domino = new Domino();
		
		public function getCounter(count:uint) {
			this.numCounter = count;
			count = 
			if 
		
		
		
		}
								   
	}
}/*/ 
								   